{% extends 'base.html.twig' %}

{% block title %}Month index{% endblock %}

{% block body %}
    <div class="container">
    <h1>Month index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for month in months %}
            <tr>
                <td>{{ month.id }}</td>
                <td>{{ month.code }}</td>
                <td>{{ month.name }}</td>
                <td>
                    <a href="{{ path('back_office_month_show', {'id': month.id}) }}">show</a>
                    <a href="{{ path('back_office_month_edit', {'id': month.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">aucun éléments trouvés</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('back_office_month_new') }}">Create new</a>
    </div>
{% endblock %}
