<form method="post" action="{{ path('back_office_month_delete', {'id': month.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ month.id) }}">
    <button class="btn">Delete</button>
</form>
